[Skip to main content](https://ohmyposh.dev/docs/segments/system/vimode#__docusaurus_skipToContent_fallback)

On this page

## What [​](https://ohmyposh.dev/docs/segments/system/vimode\#what "Direct link to What")

Display the current Vi mode (insert / normal / visual …) in the prompt. Useful
when using ZSH [keymaps](https://zsh.sourceforge.io/Doc/Release/Zsh-Line-Editor.html#Keymaps) via `bindkey -v`, PowerShell PSReadLine
`-EditMode Vi`, or fish [vi key bindings](https://fishshell.com/docs/current/interactive.html#vi-mode-commands) so you can tell at a
glance which mode you are in.

In ZSH, adding this segment automatically registers a `zle-keymap-select` hook.
In PowerShell, it registers a PSReadLine [Vi mode change handler](https://learn.microsoft.com/en-us/powershell/module/psreadline/set-psreadlineoption#-vimodechangehandler).
In fish, it registers an [`--on-variable fish_bind_mode`](https://fishshell.com/docs/current/language.html#the-fish-bind-mode-variable) handler
(only when vi or hybrid key bindings are active). All of them re-render the
prompt every time the active mode changes.

PowerShell cursor indicator

PowerShell support sets PSReadLine's `ViModeIndicator` to `Script`, replacing
cursor-shape mode indicators such as `Cursor`. The mode is then shown by this
segment in the prompt instead, or configure the top-level [`cursor_style`](https://ohmyposh.dev/docs/configuration/general#properties)
setting to vary the cursor shape by `.Env.POSH_VI_MODE` without a custom handler.

fish mode prompt

fish support overrides `fish_mode_prompt` to hook into bind mode changes and so
that fish's built-in `[N]` / `[I]` mode indicator does not show up next to the
mode rendered by this segment. Make sure nothing else in your config overrides
`fish_mode_prompt`.

## Sample Configuration [​](https://ohmyposh.dev/docs/segments/system/vimode\#sample-configuration "Direct link to Sample Configuration")

−▢×  INSERT \_ohmyposh.dev−▢×  INSERT \_ohmyposh.dev

- json
- yaml
- toml

Copy [Add to Studio](https://ohmyposh.dev/docs/studio)

vimode segment config

9

1

2

3

4

5

6

7

8

{

"type":"vimode",

"style":"plain",

"foreground":"#ffffff",

"background":"#0077c2",

"template":" {{ if eq .Mode \\"normal\\" }} NORMAL{{ else if eq .Mode \\"visual\\" }} VISUAL{{ else }} INSERT{{ end }} "

}

## Template ( [info](https://ohmyposh.dev/docs/configuration/templates)) [​](https://ohmyposh.dev/docs/segments/system/vimode\#template-info "Direct link to template-info")

default template

```template
 {{ .Mode }}
```

### Properties [​](https://ohmyposh.dev/docs/segments/system/vimode\#properties "Direct link to Properties")

| Name | Type | Description |
| --- | --- | --- |
| `.Mode` | `string` | the normalized mode: `insert`, `normal`, `visual`, `viopp`, `replace`, or the raw keymap when unmapped |
| `.Keymap` | `string` | the raw mode value ( [`$KEYMAP`](https://zsh.sourceforge.io/Doc/Release/Zsh-Line-Editor.html#index-KEYMAP) in ZSH, e.g. `main`, `viins`, `vicmd`, `visual`, `viopp`; `viins` or `vicmd` in PowerShell PSReadLine; `viins`, `vicmd`, `visual` or `replace` in fish) |

- [What](https://ohmyposh.dev/docs/segments/system/vimode#what)
- [Sample Configuration](https://ohmyposh.dev/docs/segments/system/vimode#sample-configuration)
- [Template (info)](https://ohmyposh.dev/docs/segments/system/vimode#template-info)
  - [Properties](https://ohmyposh.dev/docs/segments/system/vimode#properties)